---
import '@/styles/news.css';
import NewsHeader from '@/components/news/NewsHeader.astro';
import NewsFooter from '@/components/news/NewsFooter.astro';

interface Props {
  title?: string;
  description?: string;
  role?: 'public' | 'admin' | 'editor';
  userName?: string;
  hideNavigation?: boolean;
}

const { 
  title = 'Fishtank News',
  description = 'Latest news and updates',
  role = 'public',
  userName,
  hideNavigation = false
} = Astro.props;

const fullTitle = title === 'Fishtank News' ? title : `${title} - Fishtank News`;

// Build dynamic CSP that includes R2_PUBLIC_URL origin
const r2PublicUrl = Astro.locals.runtime.env.R2_PUBLIC_URL || '';
let r2Origin = '';
if (r2PublicUrl) {
  try {
    r2Origin = new URL(r2PublicUrl).origin;
  } catch (e) {
    // Log warning if URL parsing fails to aid in debugging
    console.warn(`Invalid R2_PUBLIC_URL: ${r2PublicUrl}`, e);
  }
}

// Build CSP directives
const imgSrcs = [
  "'self'",
  'https://img.fishtank.news',
  ...(r2Origin ? [r2Origin] : []),
  'https://*.twimg.com',
  'https://pbs.twimg.com',
  'https://www.transparenttextures.com',
  'data:',
  'blob:'
].join(' ');

const styleSrcs = [
  "'self'",
  "'unsafe-inline'",
  'https://fonts.googleapis.com'
].join(' ');

const fontSrcs = [
  "'self'",
  'https://fonts.gstatic.com',
  'data:'
].join(' ');

const frameSrcs = [
  'https://platform.twitter.com',
  'https://twitter.com',
  'https://x.com',
  'https://syndication.twitter.com'
].join(' ');

const scriptSrcs = [
  "'self'",
  "'unsafe-inline'",
  'https://platform.twitter.com'
].join(' ');

const defaultSrcs = ["'self'"].join(' ');

const connectSrcs = ["'self'"].join(' ');

const cspContent = `img-src ${imgSrcs}; default-src ${defaultSrcs}; script-src ${scriptSrcs}; style-src ${styleSrcs}; connect-src ${connectSrcs}; font-src ${fontSrcs}; frame-src ${frameSrcs};`;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta http-equiv="Content-Security-Policy" content="img-src 'self' https://img.fishtank.news https://*.twimg.com https://pbs.twimg.com data: blob:; default-src 'self'; script-src 'self' 'unsafe-inline' https://platform.twitter.com; style-src 'self' 'unsafe-inline'; connect-src 'self'; font-src 'self' data:; frame-src https://platform.twitter.com https://twitter.com https://x.com;" />
    
    <!-- Favicons -->
    <link rel="icon" type="image/png" href="/thumbnail.png" />
    <link rel="icon" type="image/x-icon" href="/thumbnail.ico" />
    <link rel="apple-touch-icon" href="/thumbnail.png" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL('/logo.webp', Astro.url)} />
    
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={Astro.url} />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL('/logo.webp', Astro.url)} />
    
    <title>{fullTitle}</title>
  </head>
  <body>
    <NewsHeader role={role} userName={userName} hideNavigation={hideNavigation} />
    <main>
      <slot />
    </main>
    <NewsFooter />
  </body>
</html>
